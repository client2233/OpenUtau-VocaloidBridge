# 第三方依赖与分发边界

本仓库不包含 `v6api/` 第三方接口包。运行时只从用户明确填写的 API 根目录导入；其作者、许可证、来源与商业引擎使用许可须由用户自行核对，本仓库许可证不覆盖用户另行提供的文件。

`bridge/worker.py` 是本项目的调用适配层，包含乐谱及音高转换和接口调用；它不是第三方接口包，也不意味着内部接口调用已经获得权利人许可。

OpenUtau 核心 DLL 只作本地编译引用，不随仓库分发。Wine、Windows Python、numpy、pyzmq、Tk 和 .NET 单独安装，分别遵守各自许可证。

拼音符号用于兼容性转换，不分发商业手册、声库或样本。项目代码保留 LGPL 2.1 许可证，见 LICENSE。
